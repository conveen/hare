[package]
name = "hare-control-plane-model"
version = "0.1.0"
edition = "2021"

[dependencies]
hare-common-model = { path = "../hare-common-model" }
tonic = { workspace = true, features = ["codegen", "prost"] }
prost = { workspace = true }

[build-dependencies]
tonic-build = { workspace = true }
